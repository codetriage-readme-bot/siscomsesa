<%= form_with(model: section, local: true) do |form| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :section_name %>
  </div>

  <div class="field">
    <%= form.label :initials %>
    <%= form.text_field :initials, id: :section_initials %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status, id: :section_status %>
  </div>

  <div class="field">
    <%= form.label :observation %>
    <%= form.text_area :observation, id: :section_observation %>
  </div>

  <div class="field">
    <%= form.label :kind %>
    <%= form.number_field :kind, id: :section_kind %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
